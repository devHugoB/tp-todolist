<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">EDT - TODOLIST</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('task_index') }}">Mes tâches</a>
                </li>
            </ul>
            <div class="d-flex">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <a class="btn btn-secondary my-2 my-sm-0" href="{{ path('auth_logout') }}">Se déconnecter</a>
                {% else %}
                    <a class="btn btn-secondary my-2 my-sm-0" href="{{ path('auth_register') }}">S'inscrire</a>
                    <a class="btn btn-secondary my-2 my-sm-0" href="{{ path('auth_login') }}">Se connecter</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>